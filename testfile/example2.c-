/* A program to perform finding the greatest common divisor of the two. */

int gcd(int u,int v)
{
	if(v==0)return u;
	else return gcd(v,u-u/v*v);
}


void main(void)
{
	int x;int y;
	x=input();
	y=intput();
	output(gcd(x,y));
}
